/**
 * QueryLens AI — App Root
 *
 * Minimal root component. Routing will be added in a later phase.
 */

import HomePage from './pages/HomePage';

export default function App() {
  return <HomePage />;
}
